// 索引结构体定义
/* 
pub struct Index {
    repo_path: String,
    staged_files: HashSet<String>, // 暂存文件集合
}
impl Index {
	pub fn load(repo_path: &str) -> Self {
		let index_path = format!("{}/.git/index", repo_path);
		//TODO
	}

	pub fn staged_files(&mut self, path: &str){
		//TODO
	}
	pub fn unstage_file(&mut self, path: &str) {
		//TODO
	}
	fn persist(&self) {
		//TODO
	}
}
	*/